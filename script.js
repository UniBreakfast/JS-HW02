function calcRectPerimeter() {
    const a = prompt('Input first side of a rectangle')
    const b = prompt('Input second side of a rectangle')
    const perimeter = a * 2 + b * 2
    alert('The perimeter of this rectangle is ' + perimeter)
}

calcRectPerimeter()
